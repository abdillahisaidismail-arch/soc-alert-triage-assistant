server.port=8081

# ── Mailtrap SMTP (sandbox) ────────────────────────────────────────
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=6568a825385de3
spring.mail.password=4b19bffa88de40
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── Alert config ───────────────────────────────────────────────────
alert.recipient=abdillahipro@gmail.com
alert.sender=soc-assistant@mailtrap.local

# ── Brute-force detector settings ─────────────────────────────────
# Trigger triage after this many failed logins from the same IP
soc.bruteforce.threshold=5
# ...within this many seconds
soc.bruteforce.window-seconds=60
